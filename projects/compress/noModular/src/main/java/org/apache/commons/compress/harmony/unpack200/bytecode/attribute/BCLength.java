package org.apache.commons.compress.harmony.unpack200.bytecode.attribute;

final class BCLength extends AbstractBcValue {

    public final int length;

    BCLength(final int length) {
        this.length = length;
    }
}
